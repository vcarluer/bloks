//
//  GameElement.h
//  Bloks-iphone
//
//  Created by antonio Munoz on 16/01/11.
//  Copyright 2011 none. All rights reserved.
//

#import <Foundation/Foundation.h>
#import "cocos2d.h"
#import "Box2D.h"

/*

class GameElement  {
	
	
	protected b2Vec2 *mPos 	
	
	protected float32 *mAngle;
	protected float32 *mWidth;
	protected float32 *mHeight;
	protected int32 *mZOrder;
	protected SimpleAudioEngine mCurrentSound;//pas sur
	protected CCAnimation mCurrentAnimation;
	//protected Hashtable<Integer, Sound> mSoundList;
	//protected Hashtable<Integer, SpriteAnimation> mAnimationList;
	
}

// returns a Scene that contains the HelloWorld as the only child
+(id) scene;

// adds a new sprite at a given coordinate
-(void) addNewSpriteWithCoords:(CGPoint)p;


@end


class GameElement 
{
	float32 mRatio;
public:
	GLESDebugDraw();
	
	GLESDebugDraw( float32 ratio );
	
	void DrawPolygon(const b2Vec2* vertices, int32 vertexCount, const b2Color& color);
	
	void DrawSolidPolygon(const b2Vec2* vertices, int32 vertexCount, const b2Color& color);
	
	void DrawCircle(const b2Vec2& center, float32 radius, const b2Color& color);
	
	void DrawSolidCircle(const b2Vec2& center, float32 radius, const b2Vec2& axis, const b2Color& color);
	
	void DrawSegment(const b2Vec2& p1, const b2Vec2& p2, const b2Color& color);
	
	void DrawTransform(const b2Transform& xf);
	
    void DrawPoint(const b2Vec2& p, float32 size, const b2Color& color);
	
    void DrawString(int x, int y, const char* string, ...); 
	
    void DrawAABB(b2AABB* aabb, const b2Color& color);
};
*/